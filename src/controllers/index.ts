import { logout, loginController, signup } from './auth';
import getProjects from './project';
import deleteTaskById from './task';

export {
  loginController, logout, signup, getProjects, deleteTaskById,
};
